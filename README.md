## Frontend

npm install  
npm run dev

## Backend

cd server  
bash start.sh --install # Install dependencies & run (Linux/Mac)
bash start.sh           # Start server (Linux/Mac)
start.bat               # Windows

## MCP Bridge

cd mcp-bridge  
node index.mjs
